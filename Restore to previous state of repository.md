# To rollback a github repository to a specific commit:
```
git reset --hard <old-commit-id>
git push -f <remote-name> <branch-name>
```
In this repository <remote-name> = origin, <branch-name> = master
